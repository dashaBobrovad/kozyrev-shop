# kozyrev-shop

## Скрипты
- `npm run dev:css` - запуск Sass в режиме watch, компиляция styles/main.scss → main.css
- `npm run clean` - очистка директории dist и создание её заново
- `npm run build:css` - компиляция SCSS в минифицированный CSS (dist/main.css)
- `npm run build:copy:root` - копирование index.html в dist/
- `npm run build:copy:assets` - копирование содержимого папок scripts/ и assets/ в dist/
- `npm run build:copy` - копирование всех необходимых файлов в dist/
- `npm run build` - полная сборка проекта: очистка → компиляция CSS → копирование всех файлов

### Что я хотела бы доработать

1. **Добавить фоновые изображения по макету**

2. **Покрыть весь JavaScript код JSDoc-комментариями**  
   Это сделает кодовую базу понятнее, облегчит навигацию и позволит легче передавать проект другим разработчикам.  
   _(Планирую использовать ChatGPT для ускорения генерации документации.)_

3. **Подключить линтер**  
   Добавить ESLint + Stylelint для единообразного стиля кода и автоматического поиска ошибок.

4. **Реализовать подгрузку элементов каталога по клику (“Load more”)**  
   Это улучшит UX и позволит работать с большими наборами данных без перегрузки страницы.

5. **Оптимизировать загрузку ресурсов**  
   Подключать CSS и JS только там, где они реально необходимы, чтобы уменьшить вес страницы и ускорить загрузку.
